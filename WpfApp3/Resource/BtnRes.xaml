<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="{x:Type Control}" x:Key="BaseBoxItm">
        <Setter Property="FontSize" Value="17"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="BorderBrush" Value="white"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="BorderThickness" Value="2"/>

        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontFamily" Value="Yu Gothic UI Light"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border CornerRadius="6"
    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
    Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden"
    VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Control}" x:Key="BaseBox" BasedOn="{StaticResource BaseBoxItm}">
        <Style.Triggers>
            <EventTrigger RoutedEvent="GotFocus">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <ColorAnimation To="#FF2C2C2C" Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
            <EventTrigger RoutedEvent="LostFocus">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation To="White" Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.4"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="BaseBtn">
        <Setter Property="FontSize" Value="17"/>
        <Setter Property="Foreground" Value="white"/>
        <Setter Property="Background" Value="#FF2C2C2C"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="BorderBrush" Value="LightSlateGray"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontFamily" Value="Yu Gothic UI Light"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
    BorderBrush="{TemplateBinding BorderBrush}"
    BorderThickness="2"
    CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center"
    VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="15" Direction="268" ShadowDepth="3" Color="#FFCECECE"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="MainBtnAnim" BasedOn="{StaticResource BaseBtn}">
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation To="0" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Opacity)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.3"/>
                        <ColorAnimation To="#FF2C2C2C" Storyboard.TargetProperty="(Button.Foreground).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation To="1" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Opacity)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.3"/>
                        <ColorAnimation To="White" Storyboard.TargetProperty="(Button.Foreground).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.3"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="BtnRedac" BasedOn="{StaticResource BaseBtn}">
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation To="0.5" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Opacity)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation To="1" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Opacity)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type Grid}" x:Key="Grid_Anim">
        <Setter Property="Width" Value="50"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation To="100" Storyboard.TargetProperty="Width" FillBehavior="HoldEnd" Duration="0:0:0:1">
                            <DoubleAnimation.EasingFunction>
                                <QuarticEase/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation To="50" Storyboard.TargetProperty="Width" FillBehavior="HoldEnd" Duration="0:0:0:1">
                            <DoubleAnimation.EasingFunction>
                                <QuarticEase/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type Border}" x:Key="Grid_Itm_Anim">
        <Setter Property="Background" Value="White"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation To="#FF595959" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                        <ColorAnimation To="White" Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                        <ColorAnimation To="#FFD487F7" Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="styleDatePicker" TargetType="{x:Type DatePicker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePicker}">
                    <Border CornerRadius="5,5,5,5">
                        <Border.Background>
                            <SolidColorBrush Color="#FFDEDEDE" Opacity="1.0"/>
                        </Border.Background>
                        <Grid>
                            <DatePickerTextBox x:Name="PART_TextBox"
    Background="{x:Null}"
    BorderBrush="{x:Null}"
    BorderThickness="0"
    Padding="{TemplateBinding Padding}"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Center"
    Visibility="Visible">
                                <DatePickerTextBox.Template>
                                    <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                                        <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
    VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                            <ContentControl Focusable="False" IsHitTestVisible="False" Opacity="1"/>
                                            <ScrollViewer x:Name="PART_ContentHost"
    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
    Margin="0"
    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                        </Grid>
                                    </ControlTemplate>
                                </DatePickerTextBox.Template>
                            </DatePickerTextBox>
                            <Button x:Name="PART_Button" HorizontalAlignment="Right" Width="30" Style="{StaticResource BtnRedacted}">
                                <Image Source="/img/calendar.png" Width="20" Height="20"/>
                            </Button>
                            <Popup x:Name="PART_Popup"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="BtnRedacted" BasedOn="{StaticResource BaseBtn}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Effect" Value="{x:Null}"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation To="#FFE0E0E0" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation To="Transparent" Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
    FillBehavior="HoldEnd" Duration="0:0:0:0.2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>